<?xml version="1.0" encoding="UTF-8"?>
<kcfg xmlns="http://www.kde.org/standards/kcfg/1.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.kde.org/standards/kcfg/1.0
                          http://www.kde.org/standards/kcfg/1.0/kcfg.xsd">
    <kcfgfile name=""/>
    <group name="General">
        <!-- Ordered list of IANA time-zone ids. "Local" means the system time zone. -->
        <entry name="timeZones" type="StringList">
            <default>Local,America/New_York,Europe/London,Asia/Tokyo</default>
        </entry>
        <!-- Use 24-hour clock; when false a 12-hour clock with AM/PM is shown. -->
        <entry name="use24HourClock" type="Bool">
            <default>true</default>
        </entry>
        <!-- Show the seconds field (updates every second). -->
        <entry name="showSeconds" type="Bool">
            <default>false</default>
        </entry>
        <!-- Show the date under each time. -->
        <entry name="showDate" type="Bool">
            <default>true</default>
        </entry>
        <!-- Show the UTC offset for each zone. -->
        <entry name="showOffset" type="Bool">
            <default>false</default>
        </entry>
        <!-- Show the weekday for each zone. -->
        <entry name="showWeekday" type="Bool">
            <default>false</default>
        </entry>
        <!-- Font family for the widget; empty means the theme default. -->
        <entry name="fontFamily" type="String">
            <default></default>
        </entry>
        <!-- Point size of the time text; 0 means automatic (theme based). -->
        <entry name="fontSize" type="Int">
            <default>0</default>
        </entry>
        <!-- Render the time text in bold. -->
        <entry name="fontBold" type="Bool">
            <default>false</default>
        </entry>
    </group>
</kcfg>
